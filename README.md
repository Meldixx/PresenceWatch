# PresenceWatch

> Recovery note: the active plugin package currently uses a fresh main file to bypass stale Revenge plugin cache after the broken v1.6.2 rollout. The functional code is the last known-good v1.6.1 build, republished as `presencewatch-1.6.3-recovery`.

Install URL:

```text
https://raw.githubusercontent.com/Meldixx/PresenceWatch/main/install/
```

After updating, fully close Discord and launch it again. The active manifest points to `install/index9.js` with hash `presencewatch-1.6.3-recovery`.
