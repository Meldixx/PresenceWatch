name: PresenceWatch smoke test

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/smoke.yml"

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Runner check
        run: |
          echo "runner ok"
          uname -a
